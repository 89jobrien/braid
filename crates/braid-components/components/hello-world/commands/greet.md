# greet

Greet the user by name.

## Usage

```
braid run --component hello-world --command greet --arg name=<name>
```

## Description

Sends a friendly greeting. Demonstrates how commands are packaged inside a
braid component. No tool calls or external APIs required.
